# Audit — cycle 2

## Loss audit

All physical content units are covered. Written-language runtime integration intentionally does not copy the book's long pronunciation/stress inventories; they remain represented by `ROS-R59` plus SOURCE_PERIOD verification gates. Bibliographies were read as coverage/reference evidence but do not create rules.

## Overgeneralization audit

Rejected rules:

- `loanword = bad`;
- `discourse word = filler = delete`;
- `repeat = bad`;
- `long/complex sentence = bad`;
- `inversion = bad`;
- `parceling = bad`;
- `metaphor = decoration = remove`;
- `shorter = better`;
- `1979 dictionary/pronunciation list = current NORM`;
- regex for `чтобы … бы` without proving a single predication;
- broad rule against `самый лучший`, because modern usage is register-sensitive.

## Counts

- atomic observations: **63**;
- new rules: **13**;
- existing Rosenthal rules enriched: **27**;
- automation added: 0 HARD_GATE / 0 DEFAULT_MECHANICAL / 1 EXTENDED_SOFT / 0 METRIC_ONLY / 12 MODEL_ONLY.
