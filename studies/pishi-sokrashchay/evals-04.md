# Original synthetic evals ps-e052—ps-e067

Book examples are not reproduced. Expected behavior is functional, not exact-string matching.

## ps-e052 · `counterexample` · rules PS-R49
Input: Сценарий сохранения. Диалог «— Кого пригласили? — Машу.» использует нормальный контекстный эллипсис. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Неполная фраза имеет ясную дискурсивную функцию.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e053 · `counterexample` · rules PS-R51, PS-R52
Input: Сценарий сохранения. В отчёте отдельно перечислены ошибка авторизации, ошибка валидации и сетевой таймаут. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Элементы относятся к одному классу, но диагностически различны.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e054 · `counterexample` · rules PS-R53, PS-R54, PS-R55, PS-R62, PS-R63
Input: Сценарий сохранения. Авторская колонка начинает абзац короткой сценой и не заканчивает его формальным выводом, потому что смысл раскрывается в следующем абзаце. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Информационная схема абзаца не универсальна для всех жанров.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e055 · `counterexample` · rules PS-R59, PS-R60, PS-R61
Input: Сценарий сохранения. Расследование строится по хронологии, потому что порядок событий и есть главный смысл. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Тематическая/модульная перестройка уничтожит причинно-временную логику.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e056 · `counterexample` · rules PS-R64, PS-R65, PS-R66, PS-R67
Input: Сценарий сохранения. Заголовок литературного эссе образный, но не вводит читателя в заблуждение и соответствует жанру. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Навигационная буквальность не является универсальной нормой.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e057 · `counterexample` · rules PS-R70, PS-R71, PS-R73, PS-R100
Input: Сценарий сохранения. Юридическое условие требует точной текстовой формулировки и не может быть заменено картинкой или бытовой аналогией. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Выбор носителя подчинён точности и доступности.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e058 · `counterexample` · rules PS-R72
Input: Сценарий сохранения. Процедура детерминирована и не имеет известных типичных сбоев в доступном материале. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Нельзя выдумывать troubleshooting ради полноты.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e059 · `counterexample` · rules PS-R74, PS-R75, PS-R77
Input: Сценарий сохранения. Инженеру нужны P95, P99, error rate и размер выборки вместе; все числа функциональны и связи между ними явно подписаны. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Нельзя вычищать факты только из-за плотности чисел.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e060 · `counterexample` · rules PS-R79, PS-R80, PS-R81, PS-R82, PS-R97
Input: Сценарий сохранения. Технический datasheet перечисляет характеристики без маркетингового перевода, потому что читателю нужны именно параметры. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Feature-to-benefit — жанровая операция, не обязательный rewrite.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e061 · `counterexample` · rules PS-R83, PS-R84, PS-R85, PS-R86, PS-R87, PS-R88, PS-R89, PS-R90
Input: Сценарий сохранения. Короткая визитка специалиста состоит из имени, роли и контакта; ей не нужны миссия, сценарии, «изюминка» и подробный рассказ. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Рекомендации самопрезентации выбираются по задаче и объёму.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e062 · `counterexample` · rules PS-R91, PS-R92
Input: Сценарий сохранения. Профессиональная лицензия является формальным условием допуска к работе и потому релевантна сама по себе. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Не каждую регалию нужно превращать в маркетинговую историю.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e063 · `counterexample` · rules PS-R93, PS-R94, PS-R95, PS-R96
Input: Сценарий сохранения. Стандартизованный массовый отклик требует только формы и CV; отдельное письмо работодателем не запрошено. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Прицельная история не должна добавляться поверх процесса без задачи.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e064 · `counterexample` · rules PS-R98
Input: Сценарий сохранения. Аварийное сообщение намеренно следует стандартному шаблону: событие, влияние, обходной путь, следующий апдейт. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Шаблон полезен, когда он организует критически важную информацию, а не заменяет мышление.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e065 · `counterexample` · rules PS-R102
Input: Сценарий сохранения. Компания действительно имеет узкое, но реальное преимущество и может показать его без громких обещаний. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Честность не означает намеренно делать самопрезентацию слабой.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e066 · `positive_operation` · rules PS-R08, PS-R78
Input: После серии формальных сокращений текст стал рубленым: «Сервис. Работает. Быстро. Без лишнего». Исходный смысл: сервис обрабатывает заявки за 20–30 секунд. Сделай финальный review.
Expected:
- Возвращает естественную связную фразу
- Сохраняет 20–30 секунд
- Использует естественность как финальный контроль, а не оправдание для новых фактов

## ps-e067 · `positive_operation` · rules PS-R102, PS-R03
Input: Компания просит написать «мы лидеры рынка», но в исходнике есть только факт: команда обслуживает один регион и специализируется на ремонте конкретной модели оборудования.
Expected:
- Не придумывает лидерство
- Строит честное позиционирование на реальной специализации
- Не делает текст намеренно слабым
