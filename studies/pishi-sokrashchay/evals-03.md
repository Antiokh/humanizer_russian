# Original synthetic evals ps-e035—ps-e051

Book examples are not reproduced. Expected behavior is functional, not exact-string matching.

## ps-e035 · `compound` · rules PS-R93, PS-R94, PS-R95, PS-R96, PS-R97
Input: Напиши структуру отклика по вакансии: требуется Kubernetes и опыт on-call. Кандидат Kubernetes использовал 2 года, on-call не дежурил, но разбирал инциденты после факта. Не обещай оффер.
Expected:
- Связывает письмо с двумя требованиями
- Честно обозначает gap on-call и близкий опыт
- Не обещает гарантированный результат отклика

## ps-e036 · `counterexample` · rules PS-R01, PS-R02, PS-R56, PS-R57, PS-R58
Input: Сценарий сохранения. Личный дневниковый фрагмент пишется ради фиксации переживания, а не ради решения прикладной задачи читателя. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Нельзя механически навязывать деловую «пользу» жанру, где функция иная.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e037 · `counterexample` · rules PS-R03, PS-R10, PS-R33, PS-R38
Input: Сценарий сохранения. Автор пишет: «По моим наблюдениям, после обновления стало хуже», потому что у него нет общей статистики. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Атрибуция и неопределённость несут смысл; удаление превратит наблюдение в факт.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e038 · `counterexample` · rules PS-R04, PS-R05, PS-R07, PS-R99
Input: Сценарий сохранения. В договоре точный термин длиннее бытового синонима, но изменение термина меняет правовой смысл. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Простота и сокращение ниже смысловой точности.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e039 · `counterexample` · rules PS-R06, PS-R09, PS-R11, PS-R12, PS-R13, PS-R14, PS-R18, PS-R21
Input: Сценарий сохранения. В инструкции «во-первых/во-вторых» задают обязательный порядок, «например» отделяет гипотетический пример, а дата показывает реальный временной срез. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Поверхностные маркеры функциональны и не должны удаляться по списку.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e040 · `counterexample` · rules PS-R08, PS-R78
Input: Сценарий сохранения. Длинный, но ясный авторский период читается без потери связей и задаёт нужный ритм. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Read-aloud — review, а не запрет длины.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e041 · `counterexample` · rules PS-R15, PS-R16, PS-R17
Input: Сценарий сохранения. Рецензент пишет «мне было скучно» и не пытается выдать реакцию за объективный рейтинг. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Субъективная оценка сама является содержанием.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e042 · `counterexample` · rules PS-R19, PS-R20
Input: Сценарий сохранения. Стандартная формула возврата товара повторяется у многих магазинов, но точно описывает процедуру и нужна для узнаваемости. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Знакомость и неуникальность не равны бессодержательности.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e043 · `counterexample` · rules PS-R22, PS-R23, PS-R24
Input: Сценарий сохранения. Процессуальное уведомление использует официальный термин, потому что именно он определяет юридическое действие. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Регистровая и юридическая точность выше бытовой простоты.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e044 · `counterexample` · rules PS-R25, PS-R28
Input: Сценарий сохранения. В художественном диалоге герой называет оппонента «трусом»; это характеристика говорящего и конфликт сцены. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Нельзя превращать реплику персонажа в нейтральный отчёт.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e045 · `counterexample` · rules PS-R26, PS-R27, PS-R40, PS-R44
Input: Сценарий сохранения. Документация для специалистов использует привычный точный термин без определения базовых понятий. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Простота и объяснение зависят от аудитории.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e046 · `counterexample` · rules PS-R29, PS-R30, PS-R31, PS-R32, PS-R50
Input: Сценарий сохранения. Интерфейс сообщает «Платёж отклонён»: агент не важен, состояние важно, активный субъект только утяжелит строку. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Номинализация/пассив/безличность не являются дефектами по классу.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e047 · `counterexample` · rules PS-R34, PS-R35, PS-R36
Input: Сценарий сохранения. В счёте указано 19 990 ₽, а в таблице тарифов «от 500 ₽» уже сопровождается точным составом минимального тарифа. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Здесь точность и нижняя граница функциональны.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e048 · `counterexample` · rules PS-R37, PS-R39, PS-R76
Input: Сценарий сохранения. Публикация с проверяемой методикой и репрезентативной выборкой действительно поддерживает обобщение и точную статистику. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Нельзя ослаблять хорошо обоснованный claim только из-за его силы или точности.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e049 · `counterexample` · rules PS-R41, PS-R42, PS-R43, PS-R68, PS-R69
Input: Сценарий сохранения. Экспертная справка перечисляет четыре давно знакомых аудитории протокола без вводной классификации. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Новизна определяется знаниями читателя, а не количеством терминов.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e050 · `counterexample` · rules PS-R45, PS-R46, PS-R48, PS-R101
Input: Сценарий сохранения. Сложное предложение с несколькими запятыми прозрачно выражает одну причинную цепочку. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Нельзя считать знаки или союзы механическим показателем дефекта.
- Сохраняет смысл, жанр и функциональный элемент

## ps-e051 · `counterexample` · rules PS-R47
Input: Сценарий сохранения. Фраза «Аудитор считает, что риск высок» нужна именно для указания источника оценки. Нужно решить, применять ли соответствующие правила.
Expected:
- Не применяет правило механически
- Прямая версия «риск высок» подменит атрибуцию фактом.
- Сохраняет смысл, жанр и функциональный элемент
