# Original synthetic evals ps-e018—ps-e034

Book examples are not reproduced. Expected behavior is functional, not exact-string matching.

## ps-e018 · `compound` · rules PS-R45, PS-R46, PS-R101
Input: Сделай легче для чтения, не удаляя нормативные запятые: «Когда отчёт приходит после закрытия периода, который уже сверил бухгалтер, который ведёт два проекта, данные приходится проверять заново».
Expected:
- Перестраивает вложенность
- Не лечит фразу простым удалением запятых
- Сохраняет условие и повторную проверку

## ps-e019 · `positive_operation` · rules PS-R47
Input: Отредактируй без потери атрибуции: «Аудитор считает, что резерв недостаточен, потому что два риска не покрыты».
Expected:
- Сохраняет, что это мнение аудитора
- Сохраняет основание — два риска
- Не превращает мнение в факт

## ps-e020 · `compound` · rules PS-R48, PS-R49
Input: Исправь только если нужно: «Мы не только после трёх недель сверки, повторного запроса документов и консультации с юристами нашли причину, но и исправили расчёт. А потом. Отправили клиенту».
Expected:
- Сокращает дальнюю зависимость
- Собирает бессмысленную парцелляцию
- Не запрещает саму конструкцию «не только… но и»

## ps-e021 · `positive_operation` · rules PS-R51, PS-R52
Input: Отредактируй: «Мы нашли ошибки, недочёты и проблемы: две ошибки авторизации и один сетевой таймаут».
Expected:
- Убирает пустой ряд при наличии точной классификации
- Сохраняет различие двух типов сбоев
- Не заменяет технические категории синонимами

## ps-e022 · `compound` · rules PS-R53, PS-R54, PS-R55
Input: Перестрой информационный абзац: «Есть ещё один важный момент. Оплата приходит в течение двух дней. Для срочного заказа можно оплатить картой — тогда стартуем сразу. Поэтому вопрос оплаты очень важен».
Expected:
- Выдвигает полезную мысль в начало
- Убирает риторический повтор в конце
- Сохраняет различие обычной и срочной оплаты

## ps-e023 · `compound` · rules PS-R56, PS-R57, PS-R59
Input: Спроектируй структуру статьи для пользователя, который хочет восстановить доступ к аккаунту. Материал содержит причины блокировки, шаги восстановления, типичные ошибки и историю продукта.
Expected:
- Формулирует контролируемую цель помощи
- Строит задачи вокруг восстановления
- Не обязан включать историю продукта, если она не нужна

## ps-e024 · `positive_operation` · rules PS-R60, PS-R61
Input: Разбей длинную справку на три самостоятельных раздела: цена, сроки, ограничения. У цены и сроков одинаковые подпункты «обычно/срочно», у ограничений другая природа.
Expected:
- Делает цену и сроки параллельными при пользе
- Не насилует раздел ограничений той же схемой
- Разделы читаются локально

## ps-e025 · `compound` · rules PS-R62, PS-R63
Input: Отредактируй статью, где вступление: «В современном мире безопасность очень важна», а заключение: «Таким образом, безопасность очень важна». Основная часть — конкретная инструкция.
Expected:
- Убирает ритуальные оболочки либо даёт им функцию
- Не добавляет красивое заключение ради симметрии
- Не меняет инструкцию

## ps-e026 · `compound` · rules PS-R64, PS-R65, PS-R66, PS-R67
Input: Выбери заголовок для инструкции по ошибке «403 при загрузке файла». Текущий: «Тайны доступа: почему система сказала нет».
Expected:
- Для навигации предпочитает название по проблеме
- Не создаёт интригу вместо ответа
- Заголовок остаётся правдивым вне контекста

## ps-e027 · `compound` · rules PS-R68, PS-R69, PS-R71, PS-R73
Input: Объясни новичку rate limit. В исходнике есть: «API принимает максимум 100 запросов в минуту; 101-й вернёт 429».
Expected:
- Сначала делает понятной роль ограничения
- Использует данный конкретный пример
- Не придумывает внутренний алгоритм API

## ps-e028 · `positive_operation` · rules PS-R70, PS-R100
Input: Для инструкции по сборке есть точная схема подключения и длинное словесное описание тех же проводов. Предложи редактуру.
Expected:
- Рассматривает схему как основной носитель пространственной связи
- Оставляет текст для условий/доступности, где он нужен
- Не заявляет, что картинка всегда лучше

## ps-e029 · `positive_operation` · rules PS-R72
Input: В инструкции уже известно: после смены пароля мобильное приложение иногда показывает 401, пока пользователь не войдёт заново. Встрой это без новых причин.
Expected:
- Ставит типичную трудность рядом с шагом
- Даёт только известный способ решения
- Не добавляет другие причины 401

## ps-e030 · `compound` · rules PS-R74, PS-R75, PS-R77
Input: Сделай метрики понятнее инженеру: «P95 820 мс, P99 1,4 с, error rate 0,18%, 2,3 млн запросов». Контекст: отчёт о стабильности.
Expected:
- Сохраняет все релевантные точные метрики
- Не выкидывает размер выборки как скучный факт
- Не придумывает арифметическую связь между метриками

## ps-e031 · `compound` · rules PS-R79, PS-R80, PS-R81
Input: Отредактируй описание: «Батарея 80 Вт⋅ч. В нашем тесте ноутбук проработал 11 часов при просмотре видео; при игре — 2 часа».
Expected:
- Связывает характеристику с наблюдаемым сценарием
- Сохраняет ограничение по игре
- Не превращает тест в гарантию всем пользователям

## ps-e032 · `compound` · rules PS-R82, PS-R83, PS-R84, PS-R85
Input: Перепиши начало страницы консультанта: «Уважаемые бизнесмены! Я волшебник трансформаций». Дано: он финансовый консультант и помогает магазинам находить кассовые разрывы.
Expected:
- Называет понятную роль
- Формулирует специфическую пользу
- Не имитирует стереотипный язык аудитории

## ps-e033 · `compound` · rules PS-R86, PS-R87, PS-R88, PS-R91, PS-R92
Input: Сделай блок о специалисте убедительнее. Есть данные: сертифицирован по PostgreSQL; мигрировал магазин с 2 ТБ базы; не берёт Oracle-проекты, но может порекомендовать партнёра.
Expected:
- Показывает компетентность через релевантные детали
- Объясняет значение сертификата через задачу
- Честно обозначает ограничение и альтернативу

## ps-e034 · `compound` · rules PS-R89, PS-R90, PS-R98
Input: Редактируй страницу компании. В черновике обязательные секции «Наша миссия» и «Наша изюминка», но реальной миссии нет; есть конкретный принцип — не брать проекты без доступа к данным.
Expected:
- Не выдумывает миссию/яркость по шаблону
- Может поднять реальный принцип как содержательную особенность
- Не копирует книжную структуру механически
