# Golub study audit

## Completeness

- Both EPUBs read sequentially before production integration: yes.
- Coverage: all substantive NCX sections VERIFIED; bibliography-only section NO_OPERATIONAL_CONTENT.
- Atomic observations: 93 deduplicated phenomena, 372 original eval records.
- Cross-book dedup: complete; two books never become two independent votes.
- Source-vs-project provenance: explicit; 5 AI_CALQUE records are `PROJECT_DERIVED`.
- Modern-norm audit: 9 high-risk normative/historical claims independently checked.

## Loss audit

The main information lost by naive atomization would be the repeated meta-principle that surface classes are not defects by themselves. The study therefore preserves the following global principle: **identify semantic/communicative function before applying an editing operation**. Functional repetition, ellipsis, long syntax, passive, nominalization, marked vocabulary, figurative language, inversion and sound patterning have explicit preservation guards.

## Overgeneralization audit

No blanket rules are encoded for repetition, passive, nominalization, participles, gerunds, long/complex sentences, colloquial vocabulary, bookish vocabulary or unusual word order. 87/93 phenomena remain MODEL_ONLY and two are metrics. Only three independently verified, narrow NORM subsets are default mechanical.

## Source conflicts

Real period/current-status conflicts are retained for `HISTORICAL.skučat_po_variant`, `HISTORICAL.blagodarya_cause_semantics`, `HISTORICAL.verb_variant_current_usage`, and contextual treatment of `период времени`. They are not averaged into a stronger rule.

## Runtime-context audit

Mandatory model context does not grow. Compact/board load the normalized mechanical adapter automatically; contextual knowledge remains in `references/golub.md` for selective use.

## Rosenthal cycle-1 re-audit

`main` gained the first operational Rosenthal cycle during this integration. The Golub matrix was re-audited against `ROS-R01..ROS-R46`: lexical precision/collocation, paronyms, register leakage, templates, reference, word order, government, nominalization, homogeneous syntax, paired conjunctions, relative reference, participle/gerund attachment, voice and historical verification now reuse source-neutral IDs where the mechanism is genuinely the same. This is provenance convergence, not an extra independent vote for passages coauthored by Rosenthal.

## Rosenthal cycle 2 reconciliation (2026-08-20)

Fresh `main` adds Rosenthal cycle 2 («А как лучше сказать?»). Golub remains a separate source-specific rule namespace, while source-neutral phenomena are aligned where mechanisms coincide: `GOLUB-R25` ↔ `ROS-R51` borrowing need/register; `GOLUB-R44` ↔ `ROS-R53` double comparative; `GOLUB-R64` ↔ `ROS-R56` sentence-boundary function; `GOLUB-R67` ↔ `ROS-R49` word-formation register; `GOLUB-R77` ↔ `ROS-R57` image-system collision; `GOLUB-R87` ↔ `ROS-R59` historical orthoepy verification. `GOLUB-R69`, `R83`, `R84` have partial scope overlap with `ROS-R52/R58` but retain distinct phenomena. Coauthorship is provenance, not an additional independent vote.
